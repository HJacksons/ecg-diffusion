name: "Hyperparameter Tuning Sweep"
method: bayes
metric:
  name: V-MSE
  goal: minimize
parameters:
  learning_rate:
    min: 0.00003
    max: 0.003
  epochs:
    min: 100
    max: 1000
  batch_size:
    values: [16, 32, 64, 128]